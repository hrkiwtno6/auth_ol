export interface IRegistStorageInfo {
  groupId: string;
  storageInfoName: string;
  storageInfoPass: string;
  storageInfoMemo: string;
}

export interface ORegistStorageInfo {
  storageInfoId: string;
}
